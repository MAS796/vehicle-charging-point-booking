# Copy this file to frontend/android/keystore.properties and fill your real values.
# Do not commit frontend/android/keystore.properties or any keystore file.

# Path can be absolute or relative to frontend/android/app.
storeFile=../release-upload-keystore.jks
storePassword=change-me
keyAlias=release-key-alias
keyPassword=change-me
